/** Host loader entry for the browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the DSH Smoothly Model Reasoning (DSH SMR) plugin. */
export function apply(): void {}
